version: '3.7'

services:
  php-fpm:
    container_name: deployer
    build:
      context: .
      dockerfile: ./.docker/php/Dockerfile
    volumes:
      - .:/var/www/html/:rw
      - ./vendor/:/var/www/html/vendor:rw